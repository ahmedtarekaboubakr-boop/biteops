export function getAreaManagerBranches(area) {
  if (area === 'Fifth Settlement') {
    return ['Mivida', 'Leven', 'Sodic Villete'];
  }
  if (area === '6th of October') {
    return ['Arkan', 'Palm Hills'];
  }
  return [];
}
